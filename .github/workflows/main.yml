on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - id: foo
        uses: RO1303/hello-world-composite-action@v2
        with:
          who-to-greet: 'this is a message running in my 1st action'
          